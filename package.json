{
    "name": "datapulse",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend/kibana-plugin && npm install && npm run build",
        "install": "cd frontend/kibana-plugin && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}