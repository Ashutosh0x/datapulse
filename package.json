{
    "name": "datapulse",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend/kibana-plugin && npm install && npm run build",
        "install-frontend": "cd frontend/kibana-plugin && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}